package Models.Card;

public class CardCup extends Card
{
    public CardCup(int number, String image, boolean isPerico, boolean isPerica)
    {
        super(number, image, isPerico, isPerica);
    }
}
